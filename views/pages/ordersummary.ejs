<!doctype html>
<html>
    <%if (user.role === "admin") { %>
        <% include ../partials/sidebar %>
    <% } %>
<head>
    <% include ../partials/head %>
</head>
<body>

<header>
    <% include ../partials/header %>
</header>

<div class="container">
    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">

                    <!-- show message when creating transaction -->
                <% if (message.length > 0) { %>
                    <div class="alert alert-danger"><%= message %></div>
                <% } %>

                <h3><span class="fa fa-user"></span> Place an order on <%= etf.name %></h3>

                    <p>
                        <strong>id</strong>: <%= etf._id %><br>
                        <strong>email</strong>: <%= etf.name %><br>
                        <strong>password</strong>: <%= etf.ric %>
                    </p>
                    <form action="/orders/delete/<%=trans._id%>" method="post">
                        <div class="form-group">
                            <label>Creation or Redemption?</label>
                              <select name="CreateRedeem">
                                <option value="Create">Creation</option>
                                <option value="Redeem">Redemption</option>
                              </select>
                        </div>
                        <div class="form-group">
                            <label>Quantity</label>: <%= trans.quantity %>
                        </div>
                        <div>
                            <label>Settlement Date </label>
                            <input type="date" name="bday">
                        </div>

                    <button type="submit" class="btn btn-warning btn-lg">Delete Order</button>
                    </form>

            </div>
        </div>

    </div>

</div>


<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>