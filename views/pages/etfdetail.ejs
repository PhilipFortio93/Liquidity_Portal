<!doctype html>
<html>
    <%if (user.role === "admin") { %>
        <% include ../partials/sidebar %>
    <% } %>
<head>
    <% include ../partials/head %>
</head>
<body>

<header>
    <% include ../partials/header %>
</header>

<div class="container">
    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> ETF - <%= etf.name %></h3>

                    <p>
                        <strong>id</strong>: <%= etf._id %><br>
                        <strong>email</strong>: <%= etf.name %><br>
                        <strong>password</strong>: <%= etf.ric %>
                    </p>

                    <a href="/orders/<%=etf._id%>"> <button type="submit" class="btn btn-warning btn-lg">Place an Order</button></a>

            </div>
        </div>

    </div>

</div>


<footer>
    <% include ../partials/footer %>
</footer>

</body>
</html>